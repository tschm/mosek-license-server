name: pre-commit

permissions:
  contents: read

on:
  push:

jobs:
  deptry:
    runs-on: ubuntu-latest

    steps:
      # construct the environment, including uv, a venv, all dependencies
      - uses: cvxgrp/.github/actions/uv/environment@main

      - uses: cvxgrp/.github/actions/deptry@main
        with:
          source-folder: 'src/mosek_license'

  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: cvxgrp/.github/actions/pre-commit@main
